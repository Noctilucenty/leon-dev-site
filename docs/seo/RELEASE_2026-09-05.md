# Search acquisition release — September 5, 2026

## Shipped scope

- One reviewed buyer guide: [Website Builder or Custom Software? Choose the Smallest Build](https://leonbuilds.org/guides/website-builder-or-custom-software).
- Four contextual links from the services hub, websites, custom software and
  business automation pages; existing inquiry CTA and event system reused.
- Twelve canonical intent records and 54 explicit aliases, a source/render-hash
  quality gate, offline query/page analysis and 60 evidence-bounded opportunities.
- Sitemap freshness correction: unchanged pages no longer look newly edited.
- Audit, architecture, keyword system, editorial gate, implementation plan and
  measurement contract in this directory. Further ideas remain proposals.

No paid tools, outbound messages, ads, generated testimonials, lead submissions,
hosting settings, runtime dependencies, API code, durable data, secrets or
compiled homepage output changed. The guide passed the independent 9/10 editorial
review described in `SEO_CONTENT_QUALITY_GATE.md`; this is not audience or ranking evidence.

## Local verification

`npm run build:static` and `npm run check` passed: 54 Python and 119 Node tests.
The 16 SEO-specific Python tests also passed after normalizing generated CSV
line endings. Mobile 390×844 and desktop 1440×960 browser checks found no
horizontal overflow and working local navigation. No form was submitted.
`git diff --check` passed; private documents and editorial records are excluded
from the static output. Existing unrelated publication-tag warnings remain documented.

## Deployment correction and receipt

Initial repository notes incorrectly named `codex/freelance2-homepage` as the
live static target. Matching baseline bytes did not prove the watched branch.
After the initial review-branch push did not deploy, read-only Render Settings
inspection confirmed the existing service follows `main`, with Auto-Deploy
On Commit, `npm run build:static` and publish directory `dist`. The audit,
README and homepage deployment note now preserve that correction explicitly.
No host setting was changed to fit the stale note.

Normal fast-forward push: `0c983d4bb3ea94af16b3afe5817f7c143a9db338` →
`e451674788d78147067f7ff0f9a6647a7031636a`. The separate API also follows main;
its runtime and dependencies were unchanged by this static release.

[Search production workflow 33993838000](https://github.com/Noctilucenty/leon-dev-site/actions/runs/33993838000)
passed. At 21:43:39 UTC, it verified the exact public fingerprint and all 50
sitemap pages: HTTP 200, exact canonicals, no noindex. At 21:43:41 UTC,
IndexNow returned HTTP 200 for five changed canonical URLs. No duplicate manual
submission was made.

Root live-byte verification at `2026-09-05T21:44:52.208Z` independently matched
the new guide, all four linking service pages, retained homepage, robots and
sitemap to the reviewed build. Public API liveness returned HTTP 200; this checks
process availability, not email delivery or successful lead handling.

Public fingerprint:
`6aeb5a1cf7e232c0d0f2f68eb981ef4a68a6661ecdab53c3d5745efa844319e6`.
Guide HTML SHA-256:
`4d3efa2a064036bf894df638b01536cbd4238bed665865b29bb98bad4bb9540d`.

## Search and measurement boundaries

At 21:44 UTC, signed-in Search Console denied access to the `leonbuilds.org`
domain property. No ownership verification, account switch, settings change or
Google indexing request was attempted. The guide's Google indexing status and
the property's AI-search inclusion setting therefore remain unknown.

IndexNow acceptance is not Google indexing, higher ranking, qualified inquiries
or revenue. Current search volume, AI visibility and acquisition lift remain
unmeasured. Use `SEO_METRICS.md` with actual same-window exports before expanding.
